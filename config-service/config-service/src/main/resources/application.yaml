server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/samueline/microservicios-practica
          search-paths: config-data
  application:
    name: config-service